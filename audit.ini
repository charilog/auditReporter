[inputs]
ports_path = inputs/ports.txt
alerts_path = inputs/alerts.txt

[filters]
min_risk = medium          ; none|low|medium|high
only_protocol = tcp        ; tcp|udp|any

[output]
report_json = report.json
